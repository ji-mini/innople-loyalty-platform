spring:
  application:
    name: innople-loyalty-platform
  jpa:
    open-in-view: false

server:
  port: ${PORT:${SERVER_PORT:3201}}

app:
  tenant:
    header-name: ${APP_TENANT_HEADER_NAME:X-Tenant-Id}
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:3200,http://127.0.0.1:3200,http://localhost:5173,http://127.0.0.1:5173,http://52.79.229.236:8090}
    allow-credentials: ${APP_CORS_ALLOW_CREDENTIALS:false}

